{
  "name": "claude-running-agent",
  "version": "1.0.0",
  "main": "main-prod.js",
  "scripts": {
    "start": "mkdir -p /root/.claude && cp /etc/secrets/credentials.json /root/.claude/.credentials.json && node main-prod.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.0.0",
    "node-cron": "^3.0.0"
  }
}